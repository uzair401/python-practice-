# Lists can also support Nested Lists  e.g  list = [a,w,r , [1,2,3,4] , u , i , k]
#  Can disolay all lsit wihth the command *list and can also display in print function using
# you can insert some data to a list in a specefied location using insert() funciton 
# sep() function which is called sepereated function. e.g print(list, sep(" "))
# lists can also supports built in functions. e,g are append(), extend(), and 
#  to remove something from a list these functions are used. pop(), del list[index], 
